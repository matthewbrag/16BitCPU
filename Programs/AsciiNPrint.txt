//use r_a to represent memory pointer
LD 0 r_a

LABEL print_loop

    //load value from memory
    GPLD r_a, r_1

    //if value is 0 (ASCII NULL) exit
    INOP SUB, r_f, r_1, 0
    JCN ZERO, end

    //else output value
    OUT 0, r_1 
    
    //and loop again
    JMP print_loop

LABEL end
